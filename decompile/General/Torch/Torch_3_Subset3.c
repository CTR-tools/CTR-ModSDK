#include <common.h>

void Torch_Subset3(void) //TODO: add DECOMP_ prefix
{
	//TODO:

	//this file was created so that git would touch this directory.
	
	//int in_at;
	//uint in_v1;
	//uint in_t0;

	//if ((int)in_v1 < 0) {
	//	in_v1 = 0;
	//}
	//else {
	//	if (-1 < (int)(in_v1 - *(ushort*)(in_at + 0x54))) {
	//		in_v1 = (uint) * (ushort*)(in_at + 0x54);
	//	}
	//}
	//if ((int)in_t0 < 0) {
	//	in_t0 = 0;
	//}
	//else {
	//	if (-1 < (int)(in_t0 - *(ushort*)(in_at + 0x56))) {
	//		in_t0 = (uint) * (ushort*)(in_at + 0x56);
	//	}
	//}
	//*(ushort*)(in_at + 0x62) =
	//	(ushort)((in_v1 + (int)*(short*)(in_at + 0x50) & 0x3ff) >> 6) |
	//	(ushort)((in_t0 + (int)*(short*)(in_at + 0x52) & 0x100) >> 4) | 0x100;
	//return;
}